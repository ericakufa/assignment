def _init_(agent):
	def method(self):
	    a=self(a)
	def method(move):
		b=move(b)


def eat(self): # can you make it eat what is left?
    if self.environment[self.y][self.x] > 10:
        self.environment[self.y][self.x] -= 10
        self.store += 10
		
def __init__(environment,list,move,eat):
    self.environment = environment
    self.store = 0
